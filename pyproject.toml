[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]  # line length handled loosely; long f-strings are fine here

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
