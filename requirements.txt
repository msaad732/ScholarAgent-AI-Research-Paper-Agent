# Pinned to the versions verified end-to-end locally so the cloud deploy is
# reproducible. transformers/tokenizers are pinned explicitly (they arrive via
# sentence-transformers) because a newer transformers breaks ChromaDB's
# SentenceTransformerEmbeddingFunction.
streamlit==1.58.0
langchain==1.3.11
langchain-groq==1.1.3
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
langgraph==1.2.6
chromadb==1.5.9
sentence-transformers==5.6.0
transformers==5.12.1
tokenizers==0.22.2
arxiv>=2.1.0
PyMuPDF>=1.24.0
python-dotenv>=1.0.0
fpdf2>=2.7.0
